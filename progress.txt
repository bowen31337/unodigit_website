# Plan 2 — Estimator & Artifacts — progress log

Committed to git deliberately: the Plan 1 ledger lived in a gitignored worktree
directory and was destroyed when the worktree was removed. Git history survived;
the reasoning did not. This file must survive compaction, worktree removal, and
a fresh session, so it lives in the repo.

Plan:  docs/superpowers/plans/2026-08-18-ba-bot-estimator-artifacts.md
Spec:  docs/superpowers/specs/2026-08-18-ba-bot-design.md  (binding authority)
State: prd.json  (per-story pass/fail — the resume point)

## Baseline at start

Plan 1 merged. Worker suite 68 tests / 8 files, zero warnings. Root
`pnpm build` emits apps/web/out with 22 pages. Never regress either.

## Codebase patterns an implementer must know

- Monorepo: pnpm workspace + Turbo 2. Worker at apps/ba-bot-api, shared wire
  types at packages/ba-bot-contract, Next site at apps/web.
- `pnpm test -- <name>` does NOT narrow under vitest 4. Run the full suite (~2s).
- Tests: `import { env, exports } from 'cloudflare:workers'`; invoke via
  `exports.default.fetch(input, init?)` — pre-bound, no env/ctx args.
  `cloudflare:test` is deprecated for `env` (still correct for applyD1Migrations).
- Mocks MUST build Response lazily: `mockImplementation(async () => new Response(...))`.
  The eager `mockResolvedValue(new Response(...))` fails under workerd with
  "Cannot perform I/O on behalf of a different request".
- `step()` returns `slots`/`forcedAdvances` BY REFERENCE when unchanged. Never
  push or assign into them; always spread.
- Session persistence goes through `persistSession()` in src/session.ts, which
  writes KV and D1 together. Writing only one is a defect class already fixed
  twice; do not reintroduce it.
- D1 enforces foreign keys (PRAGMA foreign_keys = 1). Ordering matters: check a
  row exists before writing anything that references it.

## Constraints that are not negotiable

- NO lead field (name, email) may reach src/llm/, src/graph/prompts.ts, or
  src/estimator/. DeepSeek is hosted in China; Australian Privacy Act APP 8 makes
  this the highest-consequence constraint in the project.
  Guard: grep -rnE "\b(name|email)\b" src/llm/ src/graph/prompts.ts src/estimator/
  must return only the prompt line telling the model NOT to ask.
- The frozen system prompts must stay byte-identical across requests. No
  timestamps, ids, or interpolation — DeepSeek's prefix cache is a byte match and
  ~98% cheaper on hits.
- Test output must stay pristine. Zero warnings.

## Known placeholders — must be settled before any client sees a quote

- RATE_PER_TASK_AUD = 10          (wrangler.toml, placeholder)
- MINIMUM_ENGAGEMENT_AUD = 6000   (wrangler.toml, placeholder)
- TASKS_PER_WEEK = 25             (UNCALIBRATED — this is a delivery commitment)
- src/estimator/calibration.ts    (plausible placeholders, NOT real claw-forge
                                   plan output; the whole estimate is only as
                                   good as this constant)

## Carried forward to Plan 3 as LAUNCH BLOCKERS

- Turnstile + rate limiting on POST /api/chat (spec section 10). Deferred because
  enforcement needs a client-supplied token that does not exist until the widget
  does. Without it /api/chat is open to unmetered DeepSeek spend.
- DELETE /admin/lead/:id (spec section 8). Privacy Act deletion path; must sit
  behind Cloudflare Access, which does not exist until the dashboard does.

## Log

(iterations append below)

### 2026-08-18 — US-001: Contract additions

Appended CategoryNameSchema, ConfidenceSchema, EstimateCategorySchema,
SubsystemSchema, EstimateShapeSchema (discriminatedUnion on `mode`), QuoteSchema,
and GenerateResponseSchema to packages/ba-bot-contract/src/index.ts, verbatim from
the plan's Task 1. Transcription only, no deviation.

Verification: contract typecheck exit 0, Worker typecheck exit 0, Worker suite
still 68 tests / 8 files / zero warnings, grep for cloudflare|D1Database|KVNamespace
in packages/ba-bot-contract/src returned zero matches.

Nothing to relearn here beyond what's already in this file — StateIdSchema (used
by GenerateResponseSchema) already existed at the top of index.ts, so no new
import was needed. Next implementer (Task 2, pricing) can import EstimateShape,
Quote, CategoryName directly from '@unodigit/ba-bot-contract'.

### 2026-08-18 — US-002: Category-weighted banded pricing

Created two pure modules (no I/O, no LLM):
- src/estimator/categories.ts: CATEGORY_WEIGHTS Record mapping all 7 claw-forge
  categories to commercial cost multipliers (Integrations 1.5 down to UI/UX 0.8).
- src/pricing/quote.ts: weightedTasks() sums bullets × category weight across both
  single and program modes; priceQuote() applies confidence-dependent band widths
  (high ±25%, medium ±35%, low ±50%) and flags belowFloor when midpoint < minimumAud.
  Math.max(1, ceil(weighted / tasksPerWeek)) ensures weeks ≥ 1.

All 12 test cases from the plan pass verbatim. Suite now 80 tests / 9 files / zero
warnings. Typecheck clean. Purity verified: no imports from src/db/, src/llm/, or
any I/O-bearing module. A later story (Task 3) will import these and use them to
price estimates.

### Deferred minor (from US-002 review)

Rounding order in priceQuote is asserted by code inspection only, not by any
test. With rateAud=10 and weights that are multiples of 0.1, every midpoint in
all 12 fixtures lands on a whole number, so "round then band" and "band then
round" are mathematically indistinguishable in the suite. A fixture with a
fractional midpoint (e.g. rateAud 12.5) would pin it.

NOT fixed: the divergence is cents on a four-figure quote, and the band is
labelled indicative anyway. Recorded so a future refactor of the rounding order
is a known-unpinned change rather than a surprise. Plan-level fixture gap, not an
implementer defect.

### 2026-08-18 — US-003: Shape estimator, single mode

Created src/estimator/calibration.ts (frozen CALIBRATION with three reference
decompositions), src/estimator/prompt.ts (frozen ESTIMATOR_SYSTEM_PROMPT +
PROGRAM_MODE_ADDENDUM, composed only from the frozen CALIBRATION constant — no
per-request interpolation), and src/estimator/estimate.ts (runEstimate: sends
the frozen prompt as the first message, checks finishReason === 'length' before
empty content before parse, retries malformed JSON exactly once via a REPAIR
message, and rejects a shape whose per-category bullet sum disagrees with
total_tasks as unparseable rather than trusting either number). All transcribed
verbatim from the plan's Task 3.

Verification: suite now 88 tests / 10 files / zero warnings (was 80/9);
apps/ba-bot-api typecheck clean. TDD: wrote test/estimator/estimate.test.ts
first, confirmed RED (Cannot find module '../../src/estimator/estimate', 80
baseline tests still passing), then added the three source files and confirmed
GREEN (88/10).

PII grep `grep -rnE "\b(name|email)\b" src/llm/ src/graph/prompts.ts
src/estimator/` returns four lines, not one: the expected prompts.ts
do-not-ask line, plus three under src/estimator/ that are false positives of
the same class the plan already anticipated for category names —
prompt.ts's JSON schema field `"name": string` (EstimateCategory.name /
Subsystem.name, a category label, not a lead's name) appears twice, and
calibration.ts's prose "email + SMS reminders" (describing a reference
project's notification feature, not a lead's email address). No lead field
(a visitor's actual name or email value) is read, threaded, or interpolated
anywhere in src/estimator/. Reported verbatim rather than filtered, per
instructions.

Next implementer (Task 4, program mode) extends runEstimate in-place: when a
single-mode result's total_tasks exceeds programThreshold, re-ask using
PROGRAM_MODE_ADDENDUM (already written, unused until Task 4) and return the
program-mode shape instead.

### 2026-08-19 — US-004a: oversized first passes can now reach program mode

CONTROLLER RULING, not a plan task. Plan 2 has no Task 4a; this fixes a defect
found reviewing US-004.

Defect. The spec (design doc ~line 245) says "when the first-pass estimate
exceeds 300 tasks, the estimator re-runs in program mode", with no upper bound.
But US-001's contract capped single-mode total_tasks and EstimateCategorySchema
.bullets at max(400), and runEstimate only reaches the program pass via
`first.shape.total_tasks > programThreshold` — which requires the first pass to
have VALIDATED. So a project the model sized over 400 failed schema validation,
burned its one repair retry, and returned {ok:false, reason:'parse'}; program
mode never fired. Program mode worked across a 100-task window (301-400) while
the failure zone above 400 was unbounded — and the frozen prompt steers into it
("platforms are larger"). Program-mode total_tasks already had NO max, which is
the tell: large numbers were always meant to live there.

Ruling: spec beats plan. Raised the single-mode ceilings so an oversized first
pass is representable and can route to the program pass:
- EstimateCategorySchema.bullets: max(400) -> max(2000)
- single-mode total_tasks:        max(400) -> max(2000)
- SubsystemSchema.total_tasks:    UNCHANGED at max(400) — the spec targets
  80-250 tasks per subsystem, so 400 is a correct per-subsystem bound.
Frozen ESTIMATOR_SYSTEM_PROMPT, PROGRAM_MODE_ADDENDUM and CALIBRATION are
byte-unchanged (verified by empty `git diff --stat` on those paths). Cost if the
ruling is wrong: one small revert.

Second defect, found while making the first and caused to widen by it.
totalsAgree() in program mode summed ALL subsystem category bullets and compared
that to the UMBRELLA total. It never checked that a subsystem's own categories
sum to its own total_tasks. So one subsystem could overstate and another
understate, netting out to a valid grand total. Previously bounded (categories
capped at 400); raising to 2000 widened it, and US-005's renderer prints
per-subsystem totals as delivery phases, so an inconsistent subsystem becomes a
phase whose task count contradicts its own breakdown. totalsAgree now requires
each subsystem to agree with itself, then sums subsystem totals against the
umbrella.

Verification. Suite 96 tests / 11 files, zero warnings (was 92/11); both
apps/ba-bot-api and packages/ba-bot-contract typecheck exit 0. RED was confirmed
for BOTH fixes by reverting the source and re-running, not by assertion:
- ceiling: with max(400) restored, exactly one test failed — the 600-task first
  pass returned ok:false, never reaching program mode.
- per-subsystem: with the old totalsAgree restored, the self-contradictory shape
  was ACCEPTED and returned ('expected program to be single') — i.e. it would
  have been quoted to a visitor.

Note for a later story, NOT fixed here: askOnce uses maxTokens 1600. A
program-mode shape with 6 subsystems is far larger than a single-mode one, and
deepseek-v4-flash spends most of its completion budget on reasoning tokens (a
measured chat turn: 487 completion, 356 of them reasoning — which is why
llm/turn.ts is at 4000). If a program pass truncates, askOnce returns
'truncated' and runEstimate falls back to the valid oversized single estimate,
so it degrades gracefully rather than failing — but the program artifact is
silently lost. Worth calibrating against a real 6-subsystem response before
launch.

### 2026-08-18 — US-004: Estimator program mode

Refactored the Task 3 body of runEstimate into a private askOnce(client, model,
initialMessages) (identical retry-once-on-malformed-JSON logic, just parameterized
on the message list instead of hardcoding it), then rewrote runEstimate to call
askOnce for a first pass; if ok and total_tasks <= programThreshold, returns it
unchanged (exactly one LLM call). If over threshold, calls askOnce again with
[ESTIMATOR_SYSTEM_PROMPT, PROGRAM_MODE_ADDENDUM, user] — the frozen prompt stays
first in both passes so the cached prefix is shared, and nothing per-request is
interpolated into either. A failed second pass returns `first` (the already-
validated single result) rather than erroring. Token usage on the two-pass path
sums first+second promptTokens/completionTokens. Transcribed verbatim from the
plan's Task 4 Step 3.

Verification: suite now 92 tests / 11 files / zero warnings (was 88/10);
apps/ba-bot-api typecheck clean (tsc --noEmit, exit 0, no output).

TDD: wrote test/estimator/program.test.ts first (RED), ran it against the
unmodified Task-3 estimate.ts — 2 of 4 failed as expected ("re-asks in program
mode": expected 'program', got 'single'; "sums token usage": expected 1000, got
500). Implemented Task 4's code, reran — GREEN, all 4 pass, full suite 92/11.

Deviation from the plan (reported, not silently applied): the plan's literal
`bigSingle` fixture (`bullets: 540`, `total_tasks: 540`) is schema-invalid —
`EstimateCategorySchema.bullets` and single-mode `total_tasks` are both capped at
max(400) in packages/ba-bot-contract/src/index.ts (lines 167, 191), landed in
Task 1. Verified empirically (not just by inspection): even after implementing
Task 4's code exactly as specified, tests 3 ("keeps the oversized single result
if the program pass fails") and 4 ("sums token usage") still failed, because
askOnce can never return ok:true with shape.mode:'single' for content that
violates those maxes — no implementation of runEstimate can satisfy them as
literally written. Root-caused by temporarily reverting estimate.ts (git stash)
and confirming test 1 also passed at baseline for the wrong reason (the existing
repair-retry mechanism, not the new addendum-based second pass, was consuming
the stub's second response). Fixed by changing bigSingle to
bullets/total_tasks: 350 — still > programThreshold (300), still schema-valid —
and updating test 3's one dependent assertion (540 -> 350). Re-verified RED
(reverted estimate.ts again): tests 1 and 4 now fail for the correct reason
(missing second-pass logic), tests 2 and 3 pass at baseline as expected (test 2
needs no new behavior; test 3 passes vacuously since baseline never attempts a
second pass) — then re-verified GREEN and traced test 3's call sequence by hand
to confirm the fallback path is genuinely exercised (1 successful first-pass
call + 2 failed second-pass attempts = 3 calls, not vacuous).

Worst-case total LLM calls across both passes: 4 (2 attempts in the first
askOnce, at most 2 more in the second). The under-threshold path is exactly 1
call, asserted by client.calls === 1 in program.test.ts and estimate.test.ts;
traced to confirm it cannot pass for the wrong reason (no retry needed, no
second pass invoked).

PII grep `grep -rnE "\b(name|email)\b" src/llm/ src/graph/prompts.ts
src/estimator/` still returns exactly the same four known-adjudicated lines as
US-003 (prompts.ts do-not-ask line; calibration.ts's "email + SMS reminders"
prose; two prompt.ts JSON-schema "name": string fields). Nothing in this story's
diff touches prompt.ts or calibration.ts content, so no new PII surface.

Next implementer (Task 5, brief and quote renderers) is unaffected by the
bigSingle fixture correction — it works from a self-contained EstimateShape
fixture in test/render/render.test.ts, not from estimate.ts's test fixtures.
Worth knowing: the single-mode total_tasks/bullets max(400) ceiling from Task 1
is tighter than the "100-300 typical" language in the estimator prompt, which is
intentional headroom for the over-threshold-but-still-single-mode window
(program threshold 300 < schema max 400), not a bug — only the plan's own 540
literal was out of range.

### 2026-08-19 — US-005: Brief and quote markdown renderers

Created two pure modules, transcribed verbatim from the plan's Task 5:
- src/render/brief.ts: buildBriefSections(slots) -> BriefSections, degrading
  every missing/empty slot to a visible "_Not captured during the interview._"
  placeholder rather than omitting the heading; renderBrief(sections,
  projectName) -> markdown. Never reads a lead field — only project_name,
  audience, problem, solution_summary, differentiator, personas, mvp_must,
  mvp_wont, timeline, budget_band, stack_preference, integrations.
- src/render/quote.ts: phaseOrder(subsystems) — Kahn-style topological sort
  that appends any subsystems left over (a cycle, or an unknown depends_on
  name) in declaration order rather than throwing or looping; renderQuote(...)
  -> markdown, gating the band/breakdown on q.belowFloor (a starter-engagement
  message replaces the band entirely) and the per-task-rate section on
  `rateShown && !belowFloor`.

TDD: wrote test/render/render.test.ts first (verbatim from the plan), ran
against a nonexistent src/render/ — RED for the expected reason (Cannot find
module '../../src/render/brief'). Implemented both files verbatim from the
plan, reran — 2 of 10 new tests failed unexpectedly ("shows the band and the
task count" expected 601/1247 in the output; "includes the per-task rate only
when rateShown is true" expected 'per task' to be present). Root cause: the
plan's own shared `quote` fixture sets `belowFloor: true` unconditionally, but
per the deliberate design (belowFloor replaces the band and suppresses the
per-task-rate section — see plan's "six things deliberate" #3/#4), that
fixture value is incompatible with 4 of the 6 sibling tests in the same
describe block, which assume the normal band-showing path. This is the same
class of defect as US-004's `540` fixture: the plan's literal test data
contradicts the plan's own specified behavior. Fixed minimally, preserving
each test's intent: flipped the shared fixture's default to `belowFloor:
false`, and overrode it to `true` only inside the one test that actually wants
the floor-message path ("flags a project below the engagement floor"). Reran —
GREEN, all 10 pass.

Self-review verification beyond the plan's own assertions (run as a scratch
test, then deleted before commit — not part of the committed diff): a
temporary test asserted that with belowFloor:true and rateShown:true, the
output contains neither '601' nor '1247'/'1,247' nor 'per task' — i.e. the
band and rate are genuinely ABSENT, not merely that the starter-engagement
message is present alongside them. Passed.

Dependency-cycle hand-trace (A depends_on [B], B depends_on [A]): byName =
{A,B}, done = {}. First while-iteration: for A, ready = every(d='B' ->
byName.has('B') true, done.has('B') false -> false) = false, not pushed; for B
symmetric, not pushed. progressed stays false for the whole for-loop, so the
while-loop exits after one pass (out.length 0 < 2 but progressed false). The
trailing `for (const s of subsystems) if (!done.has(s.name)) out.push(s)` then
appends A, then B — declaration order. No throw, no infinite loop; the
Phase-labelled output is simply mis-ordered rather than incomplete or crashed.

Verification: suite 106 tests / 12 files, zero warnings (was 96/11; +10
tests/+1 file for this story alone). Both apps/ba-bot-api and
packages/ba-bot-contract typecheck clean (tsc --noEmit, exit 0, no output).
Note the plan's own stated post-Task-5 total ("104 tests / 12 files") assumed
the pre-US-004a baseline of 92/11; against the actual current baseline of
96/11 the correct total is 106/12, not a regression.

Purity self-review: grep -nE "Date\.|fetch|\benv\b|\bDB\b|await" across both
new files returned zero matches. src/render/quote.ts does call `new Date(ts)`
inside day(), but `ts` is the caller-supplied `validUntil` argument, never
`Date.now()` — the function remains a pure function of its input.

PII grep `grep -rnE "\b(name|email)\b" src/llm/ src/graph/prompts.ts
src/estimator/` returns the same 4 previously-adjudicated lines as US-003/
US-004 (prompts.ts do-not-ask line; calibration.ts's "email + SMS reminders"
prose; two prompt.ts JSON-schema "name": string fields) — this story touched
neither directory. A second grep scoped to the new src/render/ files matches
only `s.name`/`c.name`/`byName` (subsystem and category labels) and a local
`name` variable in brief.ts bound to the `project_name` slot — none read a
lead's actual name or email.

### 2026-08-19 — US-005 controller hardening (not a plan task)

The renderers themselves were correct as delivered (2a7f5b8); three of their
acceptance criteria were not actually pinned by the committed suite.

1. belowFloor asserted only that a starter-engagement message APPEARS. The
   criterion is that it REPLACES the band. That test passes on a renderer
   printing both the message and "$601-$1,247" — precisely the commercially
   harmful case the branch exists to prevent. The implementer verified real
   behaviour with a scratch test and deleted it before committing, so the
   behaviour was right but unpinned. Added not.toContain for the band numbers
   and the per-task rate.
2. Phase order was unasserted. The existing test declared subsystems already in
   dependency order and checked only that both names appear and "Phase 1"
   exists, so it would pass on a renderer that ignored depends_on entirely.
   Added a test declaring Scheduling BEFORE Identity and asserting Identity is
   Phase 1.
3. No dependency-cycle test existed at all, despite being an explicit
   criterion. The implementer hand-traced the cycle in its report but never
   wrote it. Added one asserting no throw AND that both subsystems survive —
   a dropped subsystem would understate a quote a client is reading.

Suite 106 -> 108 tests / 12 files, zero warnings. Both packages typecheck.
No source change: all three passed on first run against the delivered
renderers, which is the point — they pin behaviour that was previously free to
regress silently.

Pattern worth carrying: this plan's fixtures have now been wrong three times
(US-004 total_tasks 540 vs the contract's max(400); US-005 belowFloor:true set
unconditionally on a shared fixture used by band-showing tests). Treat plan
fixtures as unverified. Prefer asserting absence over presence wherever a
requirement is phrased as "replaces", "omits", or "instead of".

### 2026-08-19 — US-006: Brief and quote persistence

Appended BriefRow, QuoteRow, BriefInsert, QuoteInsert, insertBrief, insertQuote,
getQuoteById and getBriefByConversation to src/db/queries.ts, verbatim from the
plan's Task 6. Additive only — `git diff --stat` on the file is 97 insertions, 0
deletions, so no existing query was touched. Markdown is the canonical stored
form; nothing is re-derived on read.

insertQuote counts, verified mechanically (a script parsed the INSERT column
list, counted '?' in the VALUES clause, parsed the .bind() argument list, and
compared all three against the CREATE TABLE column order in
migrations/0001_initial.sql): 15 columns / 15 placeholders / 15 binds, and the
bind order matches the column order pairwise. Every value is string|number, so
a transposition would type-check silently — hence the mechanical check rather
than a read-through.

TDD: wrote test/db/artifacts.test.ts first (7 tests), ran it — RED, all 7
failing with "TypeError: insertBrief / insertQuote / getQuoteById /
getBriefByConversation is not a function". That is only RED for "the helpers do
not exist", NOT for the three behavioural constraints, so after implementing I
re-established a targeted RED by mutation, one mutation per constraint, all
three applied at once and reverted from a backup afterwards:
- ORDER BY created_at DESC -> ASC   => only "returns the most recent brief, not
  the earlier one" failed (got the older brief id).
- swapped the row.lowAud / row.highAud binds => only "inserts a program-mode
  quote and reads it back by id" failed ("expected 6406 to be 3844") — which is
  the transposition class the plan's comment warns about, and it is now pinned.
- String(row.subsystemsJson) on the subsystems bind => only "round-trips
  subsystems_json as null in single mode" failed ("expected 'null' to be null").
Each mutation was caught by exactly one test and no other, so none of the three
assertions is vacuous.

Foreign key: the throw is genuinely the FK, not an incidental NOT NULL — every
other column is supplied in that fixture, and the error text is verbatim
`D1_ERROR: FOREIGN KEY constraint failed: SQLITE_CONSTRAINT (extended:
SQLITE_CONSTRAINT_FOREIGNKEY)`. Captured with a throwaway probe test (deleted
before commit) that also confirmed `PRAGMA foreign_keys` reads `{"foreign_keys":1}`
under the vitest pool, so FK enforcement in tests matches production. The test
asserts the message matches /FOREIGN KEY constraint failed/ AND that no orphan
row landed in `quotes` — presence of a throw alone would not prove the write did
not happen.

Two tests beyond the plan's five, both negative assertions of the kind the
US-005 hardening note asks for: getBriefByConversation returns null when a
conversation has no brief, and it returns the NEWEST brief and not the earlier
one (the plan's ORDER BY created_at DESC LIMIT 1 was otherwise unpinned — a
single-insert test passes just as well on ASC). subsystems_json is asserted
`toBeNull()` plus `not.toBe('null')`, `not.toBe('[]')` and `typeof === 'object'`,
since the dashboard distinguishes single from program mode on that column.

Verification: suite 115 tests / 13 files, zero warnings (was 108/12; +7 for this
story). Both apps/ba-bot-api and packages/ba-bot-contract typecheck clean
(tsc --noEmit, exit 0, no output). PII grep over src/llm/, src/graph/prompts.ts
and src/estimator/ returns the same 4 previously-adjudicated lines — this story
touched none of those directories, and no lead field appears in a brief or quote
row (the helpers take markdown and JSON strings the renderers produced, and the
renderers never read name/email).

Next implementer (Task 7, POST /api/generate) imports these four directly from
'../db/queries'. Note the FK ordering trap: briefs.conversation_id references
conversations(id) and quotes.brief_id references briefs(id), so the write order
is conversation -> brief -> quote, and insertBrief must have resolved before
insertQuote is attempted. getBriefByConversation is the idempotency check for
step 3 of that task — it returns the newest brief, so a retry reads back the
brief it just wrote rather than re-billing an estimate.

### 2026-08-19 — US-007: POST /api/generate orchestration

Created src/api/generate.ts (registerGenerateRoutes + an exported
endConversation), registered it in src/index.ts, and added
QUOTE_LINK_SIGNING_KEY / RESEND_API_KEY to Env — declared only, deliberately NOT
added to index.ts's REQUIRED_SECRETS, since nothing reads them yet and a 503 on
their absence would take the whole API down for a feature that does not exist.
Task 7 of the plan carries no literal code, so this is the first story written
rather than transcribed.

Order of checks, and one deliberate deviation from the plan's stated order:
404 unknown conversation -> IDEMPOTENCY -> 409 wrong_state -> brief ->
rate limit -> estimate -> price/render/persist -> advance to DONE -> close the
conversation row. The plan lists idempotency third, after the 409. That order is
wrong in practice: a successful generate leaves the session at DONE, so a
refresh or a double-click would be answered 409 rather than the brief the
visitor already paid for — contradicting the plan's own acceptance criterion
that "an existing brief for the conversation is returned". Checking the brief
first satisfies both criteria: the 409 case (mid-interview, no brief) is
unaffected, because only this route ever writes a brief.

Second judgement call: a below-floor project still returns its Quote object and
still persists the quote row. GenerateResponseSchema's doc comment in the
contract says quote is "absent when the rate limit was hit or the project priced
below the engagement floor", but the plan's step 7 prices/persists
unconditionally and its step 10 lists below-floor as a HEADLINE variant
alongside book-a-call and estimator-failed — i.e. a quote that exists but is
presented differently. renderQuote already suppresses the band and the per-task
rate for belowFloor, so the artifact is safe; returning null would leave a
non-null quoteId pointing at a row the client cannot see. The contract comment
is prose, not schema, and is now slightly stale.

`belowFloor` is NOT a column on `quotes`, so the idempotent re-read cannot load
it. quoteFromRow re-derives it with the same rule priceQuote used
(weighted * rate < MINIMUM_ENGAGEMENT_AUD). Everything else comes straight off
the row. This is the one place in the codebase that re-derives rather than
reads; a below_floor column would remove it, at the cost of a migration.

Beyond the plan's step list, deliberately: the estimate's promptTokens /
completionTokens are added to conversations.tokens_in/tokens_out via the same
UPDATE chat.ts uses. The heavy model is the most expensive call in the app and
omitting it would leave that spend invisible in the very columns that track the
cheap one. Asserted in the happy-path test (900/400), so it is not untested
scope.

TDD. Wrote test/api/generate.test.ts first: RED at module level ("Cannot find
module '../../src/api/generate'", baseline 115 still green). Then added a no-op
stub exporting both symbols so every test could fail for its OWN reason rather
than one import error, and re-ran: 12 of 13 failed with distinct observed
failures — route tests "expected 404 to be 200/409/400", the abandoned_at_state
test "expected null to be 'CONTACT'", the session test "expected 'GENERATE' to
be 'DONE'", the ended_at test "actual value must be number, received object".
The 13th ("rejects an unknown conversation with 404") PASSED vacuously against
the stub, because Hono's own not-found handler is also a 404 — fixed by
asserting the body equals {error:'not_found'}, after which it failed too.
Implemented; GREEN at 128 tests / 14 files, zero warnings (was 115/13). Both
apps/ba-bot-api and packages/ba-bot-contract typecheck clean.

Test-isolation trap worth carrying: D1 persists across tests within a file, and
`rate_limit` is keyed on (ip_hash, day). The first happy-path test called
recordQuote for the shared test IP, so every later test in the file silently
took the rate-limited branch — 5 tests failed asserting on the wrong branch, and
the ones that would still have "passed" would have been testing nothing. Fixed
with one IP per test (`203.0.113.${counter}` assigned in beforeEach). Any future
route that writes a per-IP ledger has the same hazard.

Mutation check, one at a time, each reverted from a backup:
- rate-limited branch runs runEstimate and discards the result => ONLY "returns
  the brief with no quote and makes no LLM call when rate limited" failed. The
  response shape was byte-identical, so an assertion on `quote: null` alone
  would have passed this mutant. Only the fetch-spy count caught it.
- idempotent branch runs runEstimate before returning the stored artifacts =>
  ONLY the two call-count assertions failed ("expected Mock to be called 1
  times, but got 2 times"), in the idempotency test and in the rate-limit test's
  refresh assertion. Every returned field still matched.
- headline appends "at A$77 per task" => ONLY "never puts the per-task rate in
  the headline" failed.
- endConversation binds null unconditionally => ONLY "writes abandoned_at_state
  when a session ends before DONE" failed.

The rate 77 in the tests is chosen, not arbitrary: with the fixture's 150.5
weighted tasks it yields 137 tasks / A$8,691-14,486 / 7 weeks, none of which
contain the digits "77", so `not.toContain('77')` is a real absence assertion
rather than a coincidence. The stored quote markdown is asserted to CONTAIN
"per task" and "A$77" as the positive control, so the headline's silence is a
suppression rather than a value that never existed. Band assertions use
/8,?691/-style regexes because en-AU digit grouping is not guaranteed under
workerd (render.test.ts already hedges the same way).

PII. `grep -rnE "\b(name|email)\b" src/llm/ src/graph/prompts.ts src/estimator/`
returns the same 4 previously-adjudicated lines as US-003..US-006 — no new
surface. The same grep over the new route returns exactly one line: the word
"email" inside the estimator-failed headline prose ("we will follow up by
email"), a delivery channel, not a lead field. generate.ts never touches the
`leads` table; the estimator receives `briefMarkdown`, which renderBrief builds
from the slot set only. Pinned by a test that inserts a real lead
(Jane Doe / jane@acme.com), links it to the conversation, and asserts neither
value appears in the outbound LLM request body (read off the fetch spy's call
args), the brief markdown, the quote markdown, or the headline.

Not done here, on purpose: signing (Task 8) and email (Task 9).

Next implementer (Task 8) adds src/util/sign.ts and src/api/quote.ts and moves
QUOTE_LINK_SIGNING_KEY into REQUIRED_SECRETS in the same commit that first reads
it. `getQuoteById` (US-006) is already there and unused until then.

### 2026-08-19 — US-007 controller notes

Accepted 0fd346d as delivered. Two follow-ups recorded rather than fixed.

1. Contract doc comment corrected (this commit). GenerateResponseSchema said
   `quote` is absent both when rate-limited AND when below the engagement floor.
   The implementation returns the Quote below floor, which is correct: US-005's
   renderer replaces the band with a starter-engagement message, and nulling the
   quote would make "below floor" indistinguishable from "rate limited" at the
   client. Those are different things to say to a visitor. Prose fixed, no code
   change.

2. NOT fixed — belowFloor is re-derived on the idempotent re-read.
   `below_floor` is not a column, so a second GET recomputes it as
   `weighted x rate < minimum` from the CURRENT env vars. This is the only
   re-derivation in the codebase. It is latent today and becomes real the moment
   MINIMUM_ENGAGEMENT_AUD changes — which it will, since 6000 is a documented
   placeholder: a quote rendered and stored under the old floor would come back
   with a re-derived belowFloor disagreeing with its own stored markdown. The
   markdown is the artifact the client already read, so the stored value is the
   authority. Fix is a `below_floor` column plus a migration; deferred because it
   touches schema and US-008/009 are queued behind it. Settle it before
   MINIMUM_ENGAGEMENT_AUD moves off its placeholder.

Implementer's plan deviations, both ruled correct:
- Idempotency is checked BEFORE the 409 state gate. The plan's ordering would
  409 a visitor refreshing after their brief existed, contradicting the plan's
  own idempotency criterion. Verified in source: 404 -> existing-brief -> 409.
- Estimate tokens are added to conversations.tokens_in/out, so the heavy model's
  spend is not invisible. Beyond the plan, and correct.

Test-isolation trap worth carrying: D1 persists within a test file and
rate_limit is keyed on (ip_hash, day), so tests MUST use one IP each. A shared
IP silently rate-limited five later tests in this story — they failed for a
reason unrelated to what they were testing.

### 2026-08-19 — US-008: HMAC-signed quote retrieval

Created src/util/sign.ts (signId/verifyId, HMAC-SHA256 hex over the quote id
via WebCrypto — no dependency added) and src/api/quote.ts
(GET /api/quote/:id?sig=...), registered in src/index.ts. Task 8 of the plan
carries no literal code, only an interface list, so this is written rather than
transcribed.

Constant time. verifyId delegates the MAC comparison to `crypto.subtle.verify`.
The only equality in the function is `bytes === null` on the hex-decode result,
which tests a public structural property of attacker-supplied input, never
secret material. `grep -nE "===|!==|localeCompare" src/util/sign.ts
src/api/quote.ts` returns four lines: two inside doc comments, `hex.length === 0
|| hex.length % 2 !== 0` (public length), and that null-check.

This property has NO observable behaviour — `sig === expected` and
crypto.subtle.verify return identical booleans for every input and differ only
in how long they take. Proven, not assumed: replacing verifyId's body with
`toHex(bytes) === (await signId(id, key))` left ALL 147 behavioural tests green.
So it is pinned structurally, against `verifyId.toString()` with comments
stripped — must contain 'crypto.subtle.verify', must not mention signId, must
contain no `[=!]==?` beyond the decode null-check. Re-running the same mutation
then failed all three. A `?raw` import of the source also works but matches the
word "===" in the file's own doc comments; toString sees only the shipped
function body. If a future refactor moves fromHex's guard, update the one
allowlisted `bytes === null` string in the test rather than deleting the check.

Length safety. fromHex returns null (never throws) for empty, odd-length or
non-hex input. Wrong-but-decodable lengths are deliberately NOT rejected there —
they go to crypto.subtle.verify, which returns false on a length mismatch
without throwing, keeping the comparison on the constant-time path. verifyId
also wraps the verify in try/catch: an unhandled throw becomes a 500, and a 500
is its own oracle telling an attacker their input was structurally interesting.
That catch is load-bearing, not decorative — WebCrypto rejects a zero-length
HMAC key ("Imported HMAC key length (0) must be a non-zero value..."), so an
unconfigured Worker throws inside importKey; the catch turns that into a
fail-CLOSED false. Deleting the try/catch fails exactly one test.

Enumerability. Bad signature and unknown id return the SAME status and the SAME
body: `{"error":"forbidden"}`, 403. A 404-for-unknown split would let an
attacker learn which ids exist from the status alone, and the ids are the entire
access-control mechanism. The test asserts byte identity
(`expect(await unknown.text()).toBe(await badSig.text())`), not "both are 403" —
a mutant returning 403 with `{"error":"not_found"}` fails it. The unknown-id
case is signed with the REAL key so verification passes and the miss can only
come from the D1 lookup; signing it with a wrong key would have exercised the
signature branch and tested nothing.

Verification happens BEFORE the D1 read, on purpose. Doing the lookup first
would let an unauthenticated caller drive D1 with arbitrary ids and buys no
timing symmetry that matters: producing a valid signature for an id you do not
already hold a link to requires the key, so the only branch an attacker can
reach ends at the same 403 with no query. The timing difference distinguishes
"your signature was wrong" from "your signature was right but the id is
unknown", which is not an existence oracle for anyone who cannot sign.

quoteFromRow is now EXPORTED from src/api/generate.ts and imported by quote.ts
rather than copied. belowFloor is re-derived (it is not a column) and that
re-derivation is already a recorded latent defect — US-007 controller notes. One
copy, one fix, when the below_floor column lands.

DEPLOY BLOCKER, deliberate: QUOTE_LINK_SIGNING_KEY is now in index.ts's
REQUIRED_SECRETS, per the US-007 hand-off note ("move it in the same commit that
first reads it"). An unset key HMACs every quote id under the empty string, so
the guard is exactly the "a guard becomes a no-op" class the 503 exists for.
The key does not exist in 1Password yet. It must be created
(`openssl rand -hex 32`) and pushed by scripts/sync-secrets.sh BEFORE the next
`pnpm bot:deploy`, or /api/* returns 503 for every route. SETUP.md and
sync-secrets.sh are Task 9's step 5/6 and were left untouched. Test bindings got
the key in vitest.config.ts, otherwise every existing generate/chat/contact test
would 503.

TDD. Wrote both test files first: RED at module level ("Cannot find module
'../../src/util/sign'"), baseline 128 still green. Then added no-op stubs
(signId -> '', verifyId -> false, registerQuoteRoutes -> no-op) so each test
could fail for its OWN reason, and re-ran: 15 of 19 failed with distinct
observed failures — every route test "expected 404 to be 403/200", "expected ''
to match /^[0-9a-f]{64}$/", "expected false to be true". The 4 that passed
against the stub are the negative cases, which cannot fail against a deny-all
stub; they are covered by mutation instead. GREEN at 151 tests / 16 files, zero
warnings. Both packages typecheck (tsc --noEmit, exit 0).

Mutation check, one at a time, each reverted from a backup. Every negative
assertion is pinned by at least one mutant, so none is vacuous:
- verifyId compares with `===` (re-sign and compare) => ONLY the 3 constant-time
  guard tests failed; all 147 behavioural tests stayed green. This is why the
  structural test exists.
- unknown id returns 404 => ONLY "403s on an unknown id, indistinguishably from
  a bad signature" failed ("expected 404 to be 403").
- unknown id returns 403 with `{error:'not_found'}` => same single test failed on
  the BODY ("expected '{\"error\":\"not_found\"}' to be
  '{\"error\":\"forbidden\"}'"). A status-only assertion would have survived it.
- id not bound (BOTH sign and verify use a constant message) => "rejects a
  tampered id", "rejects an empty id signed as empty", "does not accept another
  quote's signature" and "produces different signatures for different ids".
- key not load-bearing (BOTH use a constant key) => "rejects a signature made
  with a different key", the route's equivalent, and the byte-identity test.
- verifyId accepts any decodable signature => 10 tests.
- malformed signature throws instead of returning false => the length-safety
  test plus the two route tests asserting 403-not-500.
- try/catch removed => ONLY "fails closed when the signing key is unset".

The wrong-key test signs with a DIFFERENT well-formed key rather than mutating a
signature — those are distinct failure modes and only the former tests that the
key matters at all. Both are asserted separately, as is a tampered id.

Suite 128 -> 151 tests / 14 -> 16 files (+23, prd expected 132; the extra 19 are
the malformed-signature table, the three constant-time guards, and the
fail-closed test). PII grep `grep -rnE "\b(name|email)\b" src/llm/
src/graph/prompts.ts src/estimator/` returns the same 4 previously-adjudicated
lines as US-003..US-007 — this story touched none of those directories. The same
grep over the two new files matches one line, `{ name: 'HMAC', hash: 'SHA-256' }`
— a WebCrypto algorithm identifier, not a lead field. Neither new file reads the
leads table or any slot.

Next implementer (Task 9, Resend) builds the link as
`${site}/q/${quoteId}?sig=${await signId(quoteId, env.QUOTE_LINK_SIGNING_KEY)}`
and must URL-encode nothing else — the signature is lowercase hex. verifyId
accepts uppercase hex too, so a mail client that upper-cases the query string
does not lock a client out of their own quote (asserted). Task 9 still owes
SETUP.md and sync-secrets.sh for BOTH secrets.

### 2026-08-19 — US-009: Resend email delivery (Plan 2 complete)

Created src/mail/resend.ts (sendQuoteEmail — one `fetch` POST to
https://api.resend.com/emails, no SDK, no dependency added) and
src/mail/template.ts (renderEmailHtml + a ~60-line markdownToHtml). Wired into
src/api/generate.ts via a private `deliver()`, called after the quote is
persisted and the token columns are updated, before `finish()`. Task 9 carries
an interface list, not literal code, so this is written rather than transcribed.

HTML only, no attachment — the product decision. `attachments` is asserted
ABSENT from the Resend body (Object.keys + the parsed value + a JSON scan for
'application/pdf'), not merely "html is present": a body shipping a PDF
alongside the HTML passes the latter. Mutation-confirmed — adding an
attachments key fails exactly that one test.

No markdown dependency. markdownToHtml handles precisely what brief.ts and
quote.ts emit — h1-h3, `- ` lists, pipe tables (including the header rows that
have no trailing pipe), `**bold**`, `_italic_`, paragraphs. Everything is
escaped BEFORE any tag is introduced, then bold runs before italic so
`_… **x** …_` nests rather than colliding. The project name is visitor free
text that has already round-tripped through an LLM, so unescaped it is script
in the recipient's inbox; pinned by an XSS test.

PII, the reason this story exists at all. The recipient is read by a new
getLeadEmailByConversation() in src/db/queries.ts — deliberately ONE COLUMN, no
row object, so a caller cannot accidentally thread a whole lead record
somewhere it must not go. It is read INSIDE deliver(), which runs after the
estimate and after both artifacts are persisted, so the address is structurally
incapable of reaching the estimator, the renderers or any prompt: it goes from
D1 straight into the Resend envelope. Neither the address nor the lead's name
is written to the event payload — `leads` already holds it and `events` is a
support surface.

The canary test is "sends the lead address to Resend and nowhere else (PII
canary)". It seeds a lead with `canary-pii@example.invalid` /
`Canary McPiiface`, asserts the address IS in the Resend request body, and
asserts it is absent from all four of: the outbound LLM request body (read off
the fetch spy), the stored brief markdown, the stored quote markdown, and the
generate response — plus the event payload as a fifth. Not vacuous: appending
`Contact: <email>` to briefMarkdown failed it (and US-007's equivalent) on the
LLM body.

grep -rnE "\b(name|email)\b" src/llm/ src/graph/prompts.ts src/estimator/ still
returns exactly the same 4 previously-adjudicated lines as US-003..US-008. No
new surface; this story touched none of those directories.

A send failure never fails the request, and the test asserts that properly:
200 AND GenerateResponseSchema valid AND state DONE AND the brief and quote
re-read from D1 afterwards AND the quote_email_failed event AND no
quote_email_sent event. "Does not throw" would have been worth nothing.

Mutations, one at a time, each reverted from a backup:
- a Resend failure propagates (`if (!sent.ok) throw`) => ONLY the two
  send-failure tests failed, "expected 500 to be 200".
- the event log on failure removed (log only on success) => the same two tests
  failed, "expected [] to have a length of 1".
- lead email appended to briefMarkdown => ONLY the two PII tests failed
  (this story's canary and US-007's "never sends or renders a lead field").
- attachments key added => ONLY the no-attachment test.
- deliver's try/catch removed => ONLY "still returns 200 when the delivery path
  itself throws".

That last one is beyond the plan, added during self-review. sendQuoteEmail
never throws, but getLeadEmailByConversation, signId and PUBLIC_SITE_URL can.
An escaping throw would 500 a request whose brief and quote are ALREADY
committed and — worse and less obvious — skip finish(), so the session would
stay parked at GENERATE forever and the conversation row would never close.
deliver() swallows, records quote_email_failed, and the `.catch` on that log is
load-bearing: if D1 is what threw, recording the event throws too.

New var, not a secret: PUBLIC_SITE_URL ("https://www.unodigit.com.au") in
wrangler.toml + Env. The emailed link is
`${PUBLIC_SITE_URL}/q/${quoteId}?sig=${signId(quoteId, key)}`. Deliberately NOT
derived from ALLOWED_ORIGIN's first entry: that is a CORS allowlist containing
localhost, and reordering it must never change what lands in a client's inbox.
The test verifies the emitted signature against verifyId rather than pattern-
matching the URL — a wrong signature is a dead link (403) in the inbox.

RESEND_API_KEY is deliberately NOT added to REQUIRED_SECRETS. An unset key
degrades nothing that guards anything; it only stops delivery, which is already
a non-fatal, event-logged path. A 503 for the whole API because email is
misconfigured would directly contradict "a send failure must not fail the
request". sendQuoteEmail returns {ok:false,error:'no_api_key'} without making a
request, so a misconfigured deploy does not spend a guaranteed-401 round trip
per quote. Reasoning recorded in env.ts, SETUP.md and sync-secrets.sh.

SETUP.md: PUBLIC_SITE_URL added to the vars table; the secrets table is now
five with QUOTE_LINK_SIGNING_KEY; a dedicated deploy-blocker section for it
(create with openssl rand -hex 32, push before `pnpm bot:deploy`, rotation
invalidates every link already emailed); a new "Email delivery (Resend)"
section covering HTML-only/no-SDK, the quotes@unodigit.com.au sender and the
SPF/DKIM verification requirement, and how to read quote_email_sent vs
quote_email_failed. sync-secrets.sh: QUOTE_LINK_SIGNING_KEY added at
op://application/ba_bot/quote_link_signing_key.

TDD: wrote test/mail/resend.test.ts first — RED at module level ("Cannot find
module '../../src/mail/template'"), baseline 151 still green. Added no-op stubs
so each test failed for its OWN reason: 11 of 13 failed distinctly ("expected
'' to contain 'PawBook'", "expected { ok: false, error: 'stub' } to deeply
equal { ok: true, id: … }", "expected [] to have a length of 1"). The 2 that
passed against the stub are negatives that cannot fail against a deny-all stub
("makes no request when the api key is unset", "never emails when the
conversation has no lead"); both are covered by mutation instead. GREEN at
165 tests / 17 files, zero warnings. Both packages typecheck (exit 0).

Test-isolation: this file uses 198.51.100.x, one IP per test. A shared IP would
rate-limit later tests, and a rate-limited request produces no quote and
therefore no email — so these tests would have "passed" while asserting on a
branch that never ran.

DEPLOY BLOCKERS carried into Plan 3, both real today:
- op://application/ba_bot/quote_link_signing_key still does not exist.
  /api/* returns 503 on EVERY route until it is created and pushed.
- unodigit.com.au is not DNS-verified in Resend. Until SPF/DKIM are in place
  every send 422s; the visitor still gets their quote on screen, and the
  failure is visible only as a quote_email_failed row in `events`.
- The /q/ route the emailed link points at is a Plan 3 deliverable. The API
  behind it (GET /api/quote/:id) works; the page does not exist yet, so a link
  emailed before Plan 3 ships resolves to a 404 on the site.
