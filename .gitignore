# Dependencies
node_modules
.pnpm-store

# Next.js
.next
out

# Production
build
dist

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
*.pem

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Claude Code worktrees
.claude/worktrees/

# Superpowers SDD workspace (scratch: ledger, briefs, review packages)
.superpowers/

# Turborepo local task cache (root and per-package)
.turbo/

# Wrangler local state / secrets (belt and braces with apps/ba-bot-api/.gitignore —
# `pnpm wrangler ...` run one directory too high creates these at the repo root)
.wrangler/
.dev.vars
.dev.vars.*
